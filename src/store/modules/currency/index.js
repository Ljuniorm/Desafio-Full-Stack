import * as actions from './actions'
import * as mutations from './mutations'
import * as getters from './getters'

const state = {
  currencies: null,
  currency: null,
  todayDate: null
}

export default {
  state,
  actions,
  mutations,
  getters,
}
